---
title: "Quickstart"
description: "Create your first payment with the YuvexPay API in under 5 minutes."
---

## 1. Get your API credentials

Log in to the [YuvexPay dashboard](https://app.yuvexpay.com) and navigate to **Settings > API Credentials**. Create a new credential pair to get your `client_id` and `client_secret`.

<Tip>
  Start with **sandbox credentials** to test without processing real transactions. Sandbox tokens are prefixed with `sk_sandbox_`.
</Tip>

## 2. Generate an access token

Exchange your credentials for an access token:

<CodeGroup>
```bash cURL
curl -X POST https://api.yuvexpay.com/oauth/token \
  -H "Content-Type: application/json" \
  -d '{
    "client_id": "your_client_id",
    "client_secret": "your_client_secret",
    "grant_type": "client_credentials"
  }'
```

```javascript Node.js
const response = await fetch("https://api.yuvexpay.com/oauth/token", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    client_id: "your_client_id",
    client_secret: "your_client_secret",
    grant_type: "client_credentials",
  }),
});

const { access_token } = await response.json();
```

```python Python
import requests

response = requests.post("https://api.yuvexpay.com/oauth/token", json={
    "client_id": "your_client_id",
    "client_secret": "your_client_secret",
    "grant_type": "client_credentials",
})

access_token = response.json()["access_token"]
```
</CodeGroup>

The response includes an `access_token` valid for 1 hour:

```json
{
  "access_token": "sk_prod_eyJhbGciOi...",
  "token_type": "Bearer",
  "expires_in": 3600,
  "company_id": "comp_abc123",
  "company_name": "My Store"
}
```

## 3. Create a payment

Use the token to create a PIX payment:

<CodeGroup>
```bash cURL
curl -X POST https://api.yuvexpay.com/v1/payments \
  -H "Authorization: Bearer sk_prod_eyJhbGciOi..." \
  -H "Content-Type: application/json" \
  -H "X-Idempotency-Key: order-1234-attempt-1" \
  -d '{
    "amount": 49.90,
    "methods": ["PIX"],
    "currency": "BRL",
    "mode": "hosted",
    "description": "Order #1234",
    "externalId": "order-1234",
    "expiresInMinutes": 30,
    "returnUrl": "https://mystore.com/order/1234",
    "customer": {
      "name": "Maria Silva",
      "email": "maria@example.com"
    }
  }'
```

```javascript Node.js
const payment = await fetch("https://api.yuvexpay.com/v1/payments", {
  method: "POST",
  headers: {
    "Authorization": `Bearer ${access_token}`,
    "Content-Type": "application/json",
    "X-Idempotency-Key": "order-1234-attempt-1",
  },
  body: JSON.stringify({
    amount: 49.90,
    methods: ["PIX"],
    currency: "BRL",
    mode: "hosted",
    description: "Order #1234",
    externalId: "order-1234",
    expiresInMinutes: 30,
    returnUrl: "https://mystore.com/order/1234",
    customer: {
      name: "Maria Silva",
      email: "maria@example.com",
    },
  }),
});

const { payment: data } = await payment.json();
```

```python Python
import requests

response = requests.post(
    "https://api.yuvexpay.com/v1/payments",
    headers={
        "Authorization": f"Bearer {access_token}",
        "X-Idempotency-Key": "order-1234-attempt-1",
    },
    json={
        "amount": 49.90,
        "methods": ["PIX"],
        "currency": "BRL",
        "mode": "hosted",
        "description": "Order #1234",
        "externalId": "order-1234",
        "expiresInMinutes": 30,
        "returnUrl": "https://mystore.com/order/1234",
        "customer": {
            "name": "Maria Silva",
            "email": "maria@example.com",
        },
    },
)

data = response.json()["payment"]
```
</CodeGroup>

The response contains the payment details with method-specific data:

```json
{
  "payment": {
    "id": "pay_abc123",
    "txId": "YVX-20260414-ABC",
    "amount": 49.90,
    "feeAmount": 2.99,
    "netAmount": 46.91,
    "status": "PROCESSING",
    "paymentMethod": "PIX",
    "currency": "BRL",
    "description": "Order #1234",
    "expiresAt": "2026-04-14T13:30:00.000Z",
    "createdAt": "2026-04-14T13:00:00.000Z",
    "methodData": {
      "type": "PIX",
      "qrCode": "00020126...",
      "qrCodeBase64": "data:image/png;base64,..."
    }
  }
}
```

## 4. Listen for webhooks

Set up a webhook endpoint to receive payment notifications. Configure it in the dashboard under **Settings > Webhooks**.

When the customer pays, you receive a `PAYMENT_PAID` event:

```json
{
  "id": "evt_xyz789",
  "type": "PAYMENT_PAID",
  "data": {
    "id": "pay_abc123",
    "txId": "YVX-20260414-ABC",
    "status": "PAID",
    "amount": 49.90
  }
}
```

<Card
  title="Webhook guide"
  icon="arrow-right"
  href="/guides/webhooks"
>
  Learn how to verify webhook signatures and handle all event types.
</Card>

## Next steps

<Columns cols={2}>
  <Card title="API reference" icon="book" href="/api-reference/introduction">
    Explore all available endpoints.
  </Card>
  <Card title="Webhooks" icon="bell" href="/guides/webhooks">
    Set up real-time event notifications.
  </Card>
  <Card title="Error handling" icon="circle-exclamation" href="/guides/errors">
    Handle errors and edge cases.
  </Card>
  <Card title="Environments" icon="flask" href="/guides/environments">
    Understand sandbox vs. production.
  </Card>
</Columns>
